# 评审意见核查与整改答复

## 第三轮闭环结果（`outputs_review_v3`）

本轮按 48/16/16 划分训练、验证、测试，所有工况写入稳定 ID 和 SHA-256；IMC 仅用训练集整定 `lambda`，验证后冻结。最终候选在种子 101、211、307、401、503 的 80 个全新场景上密封验收。

| 算法 | 平均目标比（候选/IMC） | 单侧 95% 上界 | 非 IMC 决策 | 回退率 | 稳定率 | 结论 |
|---|---:|---:|---:|---:|---:|---|
| FNN | 0.9979 | 1.0067 | 100.0% | 0.0% | 100% | 通过 |
| RL | 0.9595 | 0.9743 | 39.88% | 0.19% | 100% | 通过 |

两种算法均无新增斜率、最低频率或启停违规，并满足“至少 10% 决策真正使用非 IMC”的防伪门。FNN 规则有效覆盖为 100%；RL 的物理热状态覆盖为 100%，测试未覆盖回退率为 0.19%。完整笛卡尔 Q 状态覆盖 46/75（61.33%），其余主要是物理不可达组合，固件绝不靠 `argmax` 默认动作，而是回退工厂 IMC。

FNN 的训练 log-RMSE 仍为 1.059，说明从局部最优标签直接拟合仍有冲突。系统因此拒绝高冲突拟合表，采用验证集选出的保守 IMC 残差 TSK 曲面；它在测试中 100% 产生非 IMC 候选，故不是用 IMC 回退冒充通过。RL 使用验证集选择的安全基线约束冻结策略。严格 Markov 问题仍只部分闭环：容量已进入 Q 状态，积分、限制器阶段和负荷被记录并参与安全判断，但真实负荷仍是估计量。

嵌入式方面，策略清单 v3 的 CRC 覆盖全部部署字段，固件启动会重算；版本、CRC 或字段异常时禁用 AI，并使用独立编译的工厂 IMC。PC-SIL 通过 75 组 Python/C++ 向量一致性验证，最大绝对误差约 `2.98e-8`。ESP32 工具链编译成功，RAM 22,036 B（6.7%）、Flash 296,169 B（22.6%）。这些仍不能代替实体 ESP32 连续 10 分钟、板端 WCET、真实传感器和 Modbus 空调测试。

可核查文件：`outputs_review_v3/dataset_manifest.csv`、`deployment_acceptance.csv`、`policy_parity_vectors.csv`、`mcu_validation_summary.csv`、`esp32_build_validation.md` 和 `review_remediation.md`。以下章节保留第一、二轮历史结论，不能用于代表 v3 的最终验收状态。

## 结论

评审提出的三类问题基本成立：旧版比较缺少统一的压缩机指令约束，训练/典型案例的传感器噪声不足，且把未经任务指标调节的极保守 IMC 回退参数直接与优化后的控制器比较，导致 IMC 被系统性低估。FNN/RL 也只能视为原型，不能仅凭生成规则表或 Q 表宣称训练完成。

本次整改后，所有算法共用相同的执行器、噪声和评估条件；IMC 只在训练集调一个 λ，测试集冻结；FNN/RL 增加独立校准回放和部署验收门。学习策略验收失败时，结果明确标为“验收拒绝→IMC”，不再把回退轨迹冒充 AI 成绩。

## 为什么旧版 IMC 很差

名义 FOPDT 辨识得到约 `K=55.16 °C/指令、τ=911.08 min、L=5 min`。旧公式取 `λ=max(τ/3,3L,12)=303.69 min`，得到约 `Kp=0.0535、Ki=5.87e-5 min^-1`。评估场景只有 4–6 h，而积分时间约 911 min，因此控制器在评估窗口内过于迟缓。

这不是“IMC 原理必然差”，而是 λ 的工程选择与任务窗口不匹配。现在保持 IMC 结构，只在训练工况搜索 λ；小规模复现实验选得 `λ=37.18 min、Kp=0.3915、Ki=0.002321`。在 4 个外部留出工况上：

| 控制器 | 平均综合目标 | 平均 ITAE | 容量指令方差 |
|---|---:|---:|---:|
| 贝叶斯固定 PI | 24.81 | 4.38 | 0.0527 |
| IMC（λ经训练集整定） | 24.68 | 4.70 | 0.0246 |
| FNN | 22.69 | 3.97 | 0.0158 |
| RL（验收拒绝后回退 IMC） | 24.68 | 4.70 | 0.0246 |

旧产物中 IMC 平均目标约 255.8；新版小规模实验为 24.68。两者同时改变了随机样本和执行器约束，不能把这个数字差直接当作严格的配对提升率。不过，`imc_lambda_tuning.csv` 在同一新版训练集内逐个评价 λ，并且新版留出集上 IMC 与贝叶斯 PI 接近、指令更平滑，足以证明旧排名主要受 λ 协议影响。正式结论仍需用默认 48/16 工况和多随机种子复核。

## FNN 的原有瑕疵

1. BO 标签是“每个工况的一组最优固定增益”，但 FNN 只输入 `e, Δe`；不同热参数/负荷可能在同一状态需要不同增益，存在状态混叠和不可约标签冲突。
2. 旧训练把样本硬塞到最近规则格，快速版只覆盖约 5/25 条规则；未覆盖区域的性能没有证据。
3. 训练轨迹来自标签控制器自身，属于离策略压缩，不能等同于端到端在线策略学习。
4. 没有独立部署验收，训练误差下降不能保证闭环指标更好。

整改：采用相邻四规则的软权重训练、IMC 先验正则、带噪测量轨迹、训练/校准切分和闭环部署验收。当前小规模实验拟合段覆盖 12/25，训练 log-RMSE 仍为 1.85，说明标签冲突依旧明显；校准回放仅比 IMC 好约 1%，虽通过 5% 非劣门槛，证据仍偏弱。外部留出表现较好只是积极信号，不足以宣称普遍优于 IMC。

## RL 的原有瑕疵

1. 旧版首个 `Δe` 随机注入，25/25 覆盖率部分来自人为构造，不代表物理轨迹真正访问。
2. 未访问 Q 状态全为 0 时，`numpy.argmax` 会选第一个动作，即 `Kp/Ki` 同时 -10%，存在隐蔽默认偏置。
3. 60 min 训练视野相对 5–12 min 延迟、执行器惯性和慢墙体状态过短；一步奖励难以正确归因。
4. 状态仅含 `e, Δe`，没有执行器状态、积分状态、负荷或热参数，不满足严格 Markov 性。
5. 增益每个热步乘法累积，容易漂到边界；奖励和最终报告综合目标也不完全一致。
6. 没有独立回放验收，策略波动时仍被直接用于测试。

整改：去掉伪造 `Δe`，未访问状态默认“不改增益”，延长到 750 回合/240 min、每 5 min 决策，将量化、斜率、启停、噪声纳入训练，并用独立校准工况比较 IMC。当前真实状态覆盖 16/25；校准目标 `56.88` 差于 IMC 的 `51.02`，所以部署门正确拒绝该策略。结论是“RL 训练仍有瑕疵且未通过”，不是“RL 已被修好并优于基线”。

## 已落实的工程约束

- 运行命令为 0 或至少 25%，量化台阶 1%。
- 非启停跃迁的容量变化率不超过 5 个百分点/min。
- 最小运行 5 min、最小停机 3 min。
- 训练、留出与典型案例加入 0.025–0.075 °C 传感器噪声和 2 min 一阶滤波。
- 时序 CSV 同时记录真实温度、测量温度、PI 请求命令和受约束后的实际命令。
- 指标新增运行斜率违规、最低频率违规、启停次数；“PWM 方差”更名为“容量指令方差”，不再把它直接称为寿命指标。
- 指令曲线使用阶梯图显示离散命令。

## 复现

小规模核查结果在 `outputs_review_final/`。命令：

```powershell
python main.py --train-samples 8 --test-samples 4 --bo-iterations 2 --seed 23 --output outputs_review_final
python -m unittest discover -s tests -v
```

正式对比应运行默认 48/16 配置，并至少增加 5 个随机种子，报告均值、标准差、95% 置信区间以及每个部署验收门的通过率。真实设备上线前，还必须用 BMS/台架数据校准 3R2C/FOPDT、最低频率和启停约束，并把 Python 执行器约束同步到 MCU 安全层。
