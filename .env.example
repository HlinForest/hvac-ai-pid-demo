# 复制本文件为 .env 并填入你的密钥；.env 已被 .gitignore 忽略，不会提交。
# 环境变量优先级：真实环境变量 > .env 文件。

# 阿里云百炼（DashScope OpenAI 兼容模式），用于 --provider openai-compatible
DASHSCOPE_API_KEY=sk-你的百炼API-KEY

# OpenAI Responses API，用于 --provider openai
# OPENAI_API_KEY=sk-...
