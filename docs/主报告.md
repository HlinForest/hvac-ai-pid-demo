# 面向机柜空调的 PI 自动整定与增益调度：统一仿真评估及软件在环验证（提交版主报告）

> 范围声明（必读）：本报告主结论限定为 E2“已冻结策略在同一 v4 仿真条件下的重评”+ E1/E3–E5 工程验证。
> E2 有两轮：B1 混合冻结重评（`sealed-80x7-v4`，v3 冻结 + legacy 保守 + replay）与
> B2 统一重训后重评（`sealed-80x7-v4-retrain`，同一 48/16/16 流水线 `v4-20260907-8f3fd5f` 全量重训冻结，legacy 双清零）。
> B2 为统一训练预算比较，B1 不是。教程式操作步骤、分算法曲线、历史批次说明分别见附件（`reports/分报告/00–12` 为教学附件，非正文）。
> 每个数字可定位到 `EXPERIMENTS.md 登记编号 + 文件 + 行/列 + code_sha`；E2 两轮证据见 `artifacts/runs/sealed-80x7-v4*/`。

## 摘要

对象为小型机柜空调单区 3R2C 热对象（执行器含延迟/惯性/ slew/量化/最小运行约束，传感器含噪声与滤波）。
工作分为离线整定/训练、冻结策略、在线增益调度、安全 PI 四段边界；LLM 不进入快速控制环，仅做离线建议。
主实验 E2 将 7 种已冻结策略放在同一 80 场景密封清单、同一噪声种子、同一六子步 v4 积分器、同一指标下重评（每轮 560 条记录）。
主统计量为逐场景配对比均值 `mean(J/J_imc)` + bootstrap 95% CI；验收为 `bounded & comfort_held & recovery_ok & actuator_compliant` 四项全过。

B1（混合冻结）主要发现：数值有界 7/7 达 80/80，但温控验收仅 Safe-BO 保守参数 51/80 明显领先，其余 38–40/80，Z-N 6/80。
性能上仅 Safe-BO 保守参数显著优于 IMC（0.9185 [0.8863,0.9516]），BO/FNN/RL/LLM-replay 与 IMC 持平区间（RL 0.9817 [0.9620,1.0070] 含 1，未证显著优；旧版 0.9664 实为均值之比，已更正）。

B2（统一重训 `v4-20260907-8f3fd5f`，48/16/16，legacy 双清零）主要发现：在同一训练预算下，
没有任何方法显著优于 IMC——BO 1.0785 [1.0424,1.1146] 显著更差（训练过拟合），Safe-BO 回落到等于 IMC（选中基线本身），
FNN 0.9920 [0.9762,1.0102]、RL 0.9801 [0.9611,1.0041] 均与 IMC 持平。这是诚实的阴性结果，见 §5 第二表。
E3 消融显示随机增益差 3–8×，保守公式差 5.9×；Safe-BO 证据支持该保守参数集，不证明 GP 搜索机制。
E3+ 同预算归因（B2 训练谱，`gp_attribution.csv`）：GP 为 BO 带来训练均值 +6.0%/密封配对比 +6.8% 的相对增益（BO-full 1.0785 vs 同初值 1.1567 vs 同预算纯随机 1.3266），
但三者绝对值均差于 IMC——GP 机制有效但不足以让 BO 在本预算下超过调好的 IMC。
E4 LLM 真实矩阵独立报告采用率与回退（v3 矩阵 5/18 部署，v4 矩阵 1/18 部署），不参与 E2 排名。
E5 PC-SIL/Python-C++ 对拍/ESP32 编译证明软件一致性，不代替实体温控实验（实体遥测/目标 WCET/HIL 三门待外部验证，本机 ESP32 编译因工具链路径含中文失败未刷新，见尝试日志）。

## 1 任务与评价目标

输入为区温误差（°C），输出为归一化制冷容量指令（0–1，容量比例）；Kp 单位为容量比例/°C，Ki 为容量比例/(°C·min)。
控制目标为综合目标 J 最小（IAE/ITAE/舒适越限/欠调/调节时间/动作量/输出方差/能耗加权，`hvac_pid/metrics.py:objective_from_metrics`），约束为安全信任域 ±10% 与执行器合规（slew/最小运行/启停）。
验收标准拆分为四项：bounded（数值有限且 5–45°C）、comfort_held（启动后持续 60 min 在 ±0.5°C 带）、recovery_ok（扰动/切温后 60 min 持续在带）、actuator_compliant；四项全过记 `validation_passed`。
回退执行不记通过，回退后仍未通过记 `fallback_failed`。`stable` 仅指数值有界，不得等同验收。

## 2 系统架构与对象模型

运行架构：上位机离线整定/训练 → 冻结策略（`PolicyBundle` 同目录版本锁定）→ 在线增益调度（FNN/RL 按状态查表/覆盖掩码回退 IMC）→ 安全 PI（含 ±10% 限幅与回退）→ 执行器（延迟/惯性/slew/量化/最小开停机）→ 3R2C 热对象 → 传感器（噪声/滤波）→ 闭环。
证据流程：实验配置（`experiments/manifests/v4.yaml` 唯一事实源）→ 数据划分（48 训练/16 验证/16 测试 + 5×16=80 密封，种子固定）→ 训练/选择 → 冻结 → 独立评估（E2）→ 统计（主/次分开）→ 报告（本主报告 + 附件）。
对象模型为 3R2C 单区，E1 已验证数值实现（六子步 N=6，初次降温 max 0.025154°C ≤0.15）与 FOPDT 代理精度（归一化 RMSE 11.88–11.95% ≤15%）。
时间口径：仿真监督 300 s、外层 60 s/内层 10 s×6 子步、演示 120 s、MCU 快环 100 ms + AI 环 2 s；LLM 离线秒级，不在快环时序内。

## 3 七类方法及统一接口

| 类 | 改什么 | 输入→输出 | 训练与部署区别 |
|---|---|---|---|
| Z-N | 一次阶跃辨识 + 经验公式 | FOPDT→固定 Kp/Ki（限幅后 1.5/0.08） | 无训练，E2 对照基线 |
| IMC | λ 一维扫描选公式参数 | FOPDT+λ→固定 0.5175/0.004054 | 21 λ 穷举，冻结即回退基准 |
| BO | GP+EI 在 log(Kp,Ki) 直接搜 | 训练 J→固定 0.5355/0.006345 | 14 评估，冻结部署 |
| Safe-BO | 风险约束下保守选点 | 训练 J+风险→固定 0.3838/0.004441（本轮为初始保守种子） | 本轮证明参数集，不证搜索机制 |
| FNN | 规则表 + 上下文系数在线调度 | 状态→Δ增益（IMC 附近 ±10%） | 离线训表，在线查表 |
| RL | Q 表 + 覆盖掩码在线调度 | 状态→Δ增益，未覆盖回退 IMC | 离线训表，在线掩码门控 |
| LLM | 离线建议 + 程序验收 gate | 工况描述→候选增益（replay 冻结等于 IMC） | E2 为 replay 冻结；真实调用见 E4 独立矩阵 |

统一接口为 `PolicyBundle`（IMC/BO/SafeBO/FNN 表+上下文/RL 表+掩码同目录冻结，缺文件报错，legacy 回退显式标记）；`run.py sealed/embedded/pipeline` 均经此加载，禁止各入口自行拼装。

## 4 实验设计与复现协议

E1 数值/FOPDT：三典型工况（初次快速降温/设定突变/持续热扰动），阈值未动，物理改为子步进后通过。
E2 密封重评：80 场景（`acceptance_seeds=101,211,307,401,503`，`seed+100003+seed_i`，噪声 `seed+700000+ordinal`），七算法同场景同种子同积分器同指标；FNN/RL 记候选 vs 执行；测试后调参换新密封集。
E3 消融：同 80 场景 4 组随机固定增益 + 保守 IMC 公式，隔离“搜索 vs 保守初值”。
E4 LLM 矩阵：v3 矩阵 18 运行/18 真实/18 有界/5 部署；v4 矩阵 18 运行/1 部署/17 回退；报告采用率、回退后验收与成本，独立成表。
E5 对拍/SIL：75 组 Python/C++ 对拍 ~2.98e-8、CRC、PC-SIL 分频、ESP32 目标编译（RAM/Flash 记录）；历史 Modelica DASSL 12 h 为历史证据，当前未复现。
统计：主 `mean(obj/imc)` + bootstrap CI（2000，seed+77），非劣界 1.02（上界≤1.02），显著优要求上界<1.0；次 `mean(obj)/mean(imc)` 仅对照。
复现：`python run.py sealed --artifact-dir <bundle> --output <run>`；一致性 `python tools/check_results_consistency.py --sealed-dir <run>`（重算均值/双统计量/CI/验收/回退定义/溯源）。

## 5 结果与失败分析

主结果（E2-B1 混合冻结轮，`sealed-80x7-v4/sealed_80_summary.csv:2-8`，性能与验收并列）：

| 算法 | 平均 J↓ | 主配对比 [95% CI] | 验收 | 结论 |
|---|---:|---|---:|---|
| Z-N | 61.72 | 1.6520 [1.5427,1.7666] | 6/80 | E2 内显著差 |
| IMC | 41.24 | 基准 | 39/80 | 基准；有界全过但验收不足半数 |
| BO | 41.95 | 0.9996 [0.9808,1.0193] | 38/80 | 与 IMC 相当，不称优 |
| Safe-BO 保守参数 | 36.72 | 0.9185 [0.8863,0.9516] | 51/80 | 本轮最优；归属为参数集 |
| FNN | 41.44 | 0.9980 [0.9886,1.0084] | 40/80 | 非劣，未证显著优 |
| RL | 39.83 | 0.9817 [0.9620,1.0070] | 39/80 | 非劣，未证显著优；4 回退后仍未过 |
| LLM replay | 41.24 | 持平 IMC | 39/80 | 等于 IMC，非实时调用 |

失败分析：有界 80/80、执行器 80/80 全过；瓶颈在 `comfort_held`（IMC 44/80→验收 39/80）与 `recovery_ok`（Safe-BO 60→51）。即 J 尚可但未持续在带——回退基准在困难谱同样不合格，否定“回退基准本身合格”。
归因：E3 随机 3–8× 更差说明非随机已是价值；Safe-BO 选中 `evaluation=2/local qualification seed`，机制未证，需同初始化同预算随机搜索对照补强（E3 已有随机对照，固定保守对照为公式默认，GP 机制对照仍缺）。
成本：BO 14、SafeBO 7 评估最轻；FNN ~4181 h、RL ~4018 h 等效对象时最重；LLM 单次 5–25 s + API 最贵。RL 回退 5%（4/80）全部记失败，FNN 无回退。

统一重训轮（E2-B2，`sealed-80x7-v4-retrain/sealed_80_summary.csv`，同一流水线冻结，`bo_legacy_fallback=0/safe_bo_legacy_fallback=0`，一致性校验通过）：

| 算法 | 平均 J↓ | 主配对比 [95% CI] | 验收 | 读法 |
|---|---:|---|---:|---|
| Z-N | 61.72 | 1.6509 [1.5407,1.7630] | 6/80 | 显著差 |
| IMC | 41.24 | 基准 | 40/80 | 基准（本轮 λ 重扫，增益与 B1 基本一致） |
| BO | 45.02 | 1.0785 [1.0424,1.1146] | 33/80 | 显著差于 IMC：14 次评估过拟合训练谱，同预算下不可靠 |
| Safe-BO | 41.24 | 1.0000 等于 IMC | 40/80 | 本轮选中基线本身（`evaluation=1/known baseline`），无增益、无损失 |
| FNN | 41.17 | 0.9920 [0.9762,1.0102] | 41/80 | 与 IMC 持平（部署验收通过，但密封未显著优） |
| RL | 39.80 | 0.9801 [0.9611,1.0041] | 39/80 | 与 IMC 持平，CI 含 1 |
| LLM replay | 41.24 | 持平 IMC | 40/80 | 等于 IMC，非实时调用 |

E3+ 同预算归因（`sealed-80x7-v4-retrain/gp_attribution.csv`，同 48 训练谱选型、同 80 密封评价）：
BO-full 密封 45.02（1.0785）vs 初值最优 init-only-7 密封 46.30（1.1567）vs 同预算纯随机 random-14 密封 49.52（1.3266）vs 保守公式 250.23（7.98×）。
GP 相对贡献为正（训练均值好 6.0%，密封配对比好约 6.8%，比纯随机好约 18.7%），但绝对值仍不如 IMC——结论是“GP 搜索机制有效，但在本预算/种子下不足以超越调好的 IMC”，
而不是“BO 无用”或“BO 最优”，两种简化读法都禁止。

## 6 软件在环与部署边界

导出：冻结策略经 `PolicyBundle` 哈希锁定，`manifest.yaml` 随 run 冻结，`sealed_scenarios.csv` 存全场景清单，`code_sha` + 脏位如实记录。
B2 轮 `policy_provenance` 中 `bo_legacy_fallback=0/safe_bo_legacy_fallback=0`，RL 掩码为冻结 `rl_covered_mask.npy`（非重建），FNN/RL 部署验收均为通过。
对拍：Python/C++ 差 ~3e-8 量级；CRC 校验；PC-SIL 按 100 ms/2 s 分频跑通（B2 run 内 `mcu_pc_sil_log.txt:PASS`，worst PI 300 ns / AI 42.2 µs 均为 HOST 二进制计时）；
主机 WCET 参考 `host_wcet.csv`（PC Python 单步约 10–230 µs，HOST-ONLY）；ESP32 目标编译在本机刷新失败
（xtensa 工具链无法处理用户路径中文字符，`esp32_target_compile_attempt.log` 存证，历史 RAM/Flash 记录维持，门仍为 PENDING）。
边界：以上证明软件一致性，不证明与真实机柜对象对应，更不代替实体 10 分钟遥测、真实 WCET、HIL、Modbus 写入
（三门 `python tools/check_hardware_gates.py` 当前 3/3 PENDING，不得仿真冒充）。
LLM 边界：E2 replay 非实时调用；E4 真实矩阵链路通≠模型好用，回退率与成本单独报告。

## 7 结论与局限

分条件结论：B1 谱内，保守 Safe-BO 参数集性能与验收双优但机制未证；B2 统一预算下，无任何方法显著优于 IMC（BO 显著差，Safe-BO 持平，FNN/RL/LLM 持平区间）；Z-N 两轮均显著差。
局限：B2 只完成单一种子单预算统一重训，多种子重复、更大 BO 预算、GP 机制“仅初始化不搜索”长期对照仍缺；实体/HIL/目标 WCET 全缺（3/3 PENDING）；
DOCX 排版以 `tools/check_docx.py` 逐节核对为准，PDF 未生成。
下一步：固定 E2 两轮批次不再覆盖（新批次换 run_id）；补多种子重复与更大预算 BO；
补 E4 真实调用矩阵的失败原因与成本明细；随后实测辨识→遥测→HIL 逐层验收，每层追溯到同一策略版本。

## 附件

- 复现指南：`README.md` + `EXPERIMENTS.md` + `run.py --help`（环境/快速运行/冻结重评/完整训练/图表/排查）；算法差异见 `docs/ALGORITHM_GUIDE.md`。
  完整重训：`python run.py pipeline --output artifacts/runs/<run_id> --seed 7`（本轮约 227 s）→
  `python run.py sealed --artifact-dir artifacts/runs/<run_id> --output artifacts/runs/<sealed_id>` →
  `python tools/run_gp_attribution.py --artifact-dir artifacts/runs/<run_id> --output artifacts/runs/<sealed_id>/gp_attribution.csv`。
- 证据附件：`artifacts/runs/sealed-80x7-v4/`（B1）+ `artifacts/runs/sealed-80x7-v4-retrain/`（B2，含 `gp_attribution.csv`）+
  `artifacts/runs/v4-20260907-8f3fd5f/`（B2 训练冻结：`bo_policy.json`/`safe_bo_policy.json`/`rl_covered_mask.npy`/`host_wcet.csv`/`mcu_pc_sil_log.txt`）+
  `artifacts/runs/v4-20260906-2870262/`（E1）+ `archive/outputs_review_v3/`（B1 冻结策略）+ `archive/outputs_llm_matrix_v3/`（E4）。
- 硬件门：`embedded/hardware_gates.md` + `python tools/check_hardware_gates.py --run <run>`（3/3 PENDING 为当前诚实状态）。
- 教学附件：`reports/分报告/00–12`（操作教程与单算法曲线，主文用同轴对比，单曲线放附件）；旧总报告/DOCX/PPT 归 `reports/legacy/`，唯一主报告为本文件（DOCX 见 `docs/主报告.docx`，核对见 `tools/check_docx.py`）。
- 实验登记：正文/图表/CSV 统一用 E1–E5；`RESULTS.md` 为 E2 主结果表可定位版本。
