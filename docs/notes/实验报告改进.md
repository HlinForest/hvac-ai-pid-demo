1. 公式没有渲染好 
2. 每个算法每个工况的的仿真结果图需要加上去
3. 

---

## 第 1 轮处理记录（2026-09-02）

### 意见 1：公式渲染 —— 已处理
- 报告全部公式改为标准 LaTeX：独立公式用 `$$...$$`、行内公式用 `$...$`（3R2C 状态方程组、Z-N/IMC 整定公式、PI 控制律、RL 奖励与 Q 更新、风险目标、综合目标 J、IAE/ITAE 等），在 VS Code/Typora 等支持 KaTeX 的环境中可直接渲染。
- `reports/md2docx.py` 扩展公式渲染：mathtext（Computer Modern 字体）→ 300 dpi PNG 嵌入 docx，独立公式居中、行内公式随文；渲染失败自动回退 Cambria Math 斜体并告警；docx 被 Word 占用时自动另存 `.new.docx`。
- 本轮共渲染 45 张公式图片（`reports/figures/equations/`），docx 无告警通过。

### 意见 2：每算法每工况仿真结果图 —— 已处理
- **实验改进**：`hvac_pid/pipeline.py` 的 `_run_case_studies` 新增 `case_timeseries.csv`（三工况 × 五算法逐分钟时序），`tests/test_demo.py` 同步更新，25 项测试全部通过。
- 新增 `reports/make_case_figures.py`：从 v3 封存策略（不改写封存产物）按封存种子复现三工况仿真，**自检复现指标与 case_metrics.csv 最大相对偏差 3.64e-16** 后才出图。
- 新增 4 张图并插入报告 6.3 节（综述版报告 3.5 节同步插入）：
  - 图 5 工况一·初次快速降温、图 6 工况二·设定温度突变、图 7 工况三·持续外界热扰动：五算法温度响应 + 容量指令双面板，标注设定值、±0.5 °C 舒适带、设定值突变/开门扰动事件；
  - 图 8 七算法统一演示（ESP32 场景）：七算法温度 + 容量指令，标注 min 189 开门窗口。
- 附录 C 图表清单、附录 B 复现命令、附录 A 数据文件索引同步更新。

### 补充（问答中发现的可读性问题）
- 术语表 "holdout" 一条改写为通俗解释：先说目的（调参不许看、冻结后开封考核、防背题），再列两道密封线（16 测试场景 + 5 种子 ×16 = 80 封存验收场景），并指明结果文件。

- §2.2 执行器/传感器约束表重写：补齐六条约束的具体取值（最短运行 5 min/停机 3 min、噪声 σ=0.04 °C 等）与逐条工程依据（教科书值 / 行业惯例 / 刻意保守项均注明）；§8 局限性新增"湿度与结霜未建模"一行。

### 产物
- 报告：`reports/实验报告_AI-PI整定.md`（docx 因原文件被 Word 占用另存为 `实验报告_AI-PI整定.new.docx`，关闭 Word 后重跑 `python reports/md2docx.py 实验报告_AI-PI整定.md` 即可覆盖）
- 脚本：`reports/make_case_figures.py`（新增）、`reports/md2docx.py`（扩展）、`hvac_pid/pipeline.py`（新增 case_timeseries.csv 输出）

---

## 第 2 轮处理记录（2026-09-02）

### 意见：建模章节弱化省略、聚焦"如何整定"、每算法单独配图 —— 已处理
（确认范围：整个第 2 章压缩；§4 整定过程图 + 15 张单算法工况图两者都要。）

- **第 2 章压缩为半页**：原 2.1–2.5（状态方程、约束表、FOPDT 辨识、整定公式推导、三层交叉验证）压缩为三段式「被控对象与实验条件」；全部细节完整移入**附录 D（D.1–D.5）**，信息不丢失。
- **§4 每个算法小节配「整定过程图」**（新增 `reports/make_tuning_figures.py`，只读封存 CSV 绘图，报告统一画风）：
  - 图 3 Z-N/IMC 数据基础：168 h 阶跃试验与 FOPDT 拟合；图 4 IMC-λ 扫描（21 候选，选中 26.9 min vs 保守回退 303.7 min）；图 5 贝叶斯 BO 搜索轨迹；图 6 安全 BO 风险目标搜索；图 7 LLM Agent 工具调用轨迹；图 8 FNN 训练（拟合误差 + 规则覆盖率，如实呈现标签冲突回升）；图 9 RL 训练（奖励/ε 衰减 + 验证集早停选表）。
- **6.3 合并对比图替换为 15 张单算法×单工况图**（改写 `reports/make_case_figures.py`）：每工况 5 张、每算法单独成图（温度+指令双面板，图题下标注该算法该工况 ITAE/调节时间/最大过冷）；封存复现自检通过（最大相对偏差 3.64e-16）。
- **全文图号按出现顺序重排为图 1–27**：图 1–2 框图不变；图 3–9 §4 整定过程；图 10–11 耗时/性能（原图 3/4）；图 12–26 逐算法工况图；图 27 七算法演示（原图 8）。`make_figures.py` 同步改名重新生成，旧编号 PNG 已删除。
- **连带修正**：IMC λ 候选数 13→21（与 v3 封存 `imc_lambda_tuning.csv` 一致）；§3.1/§4.2 对旧 2.2/2.3 节的引用改指附录 D；综述版报告 3.5 节改为指向详解版图 12–26 并仅保留图 27；大纲文档图名同步。

### 产物
- 报告：`reports/实验报告_AI-PI整定.md`（docx 另存为 `实验报告_AI-PI整定.new.docx`——原文件仍被 Word 占用，关闭后重跑 `python reports/md2docx.py 实验报告_AI-PI整定.md` 覆盖）、`reports/实验报告_AI自动整定.md`、`reports/实验报告生成.md`
- 脚本：`reports/make_tuning_figures.py`（新增）、`reports/make_case_figures.py`（改写为 15 单图）、`reports/make_figures.py`（图号 3/4→10/11）
- 图片：`reports/figures/` 图 1–27（27 张，docx 共嵌 75 张图 = 27 图 + 48 公式 PNG）
- 校验：pytest 25 项通过；docx 生成无公式渲染告警。
- 追加：应要求另存新版 docx 而不覆盖原文件——`md2docx.py` 支持第二参数指定输出名（并修正两参数模式下相对路径不按 reports/ 解析的不一致）；已生成 `reports/实验报告_AI-PI整定_v2.docx`（75 图 / 260 段 / 14 表，校验通过）。

---

## 第 3 轮处理记录（2026-09-09）

### 意见（根目录 `改进.md`）：RL 整定章节三项改进 —— 已处理

1. **声明当前任务中的 state / action / reward** —— 详解版新增 §4.8.1：三条分箱边界数值表（e ∈ [−2, −0.5, 0.5, 2] °C、ė ∈ [−0.15, −0.03, 0.03, 0.15] °C/min、u_prev ∈ [0.05, 0.70]，与 `ai_controllers.py:572-574` 一致）、9 个动作的 3×3 组合表（(Kp, Ki) 目标 ∈ {0.75, 1.00, 1.30}×IMC，动作 4 = 保持 IMC）、按误差档位的动作安全屏蔽表、奖励公式逐项物理含义表（六项各自的权重与理由）。
2. **为什么选 Value-Based 的 Q-Learning 而非 Policy-based** —— 新增 §4.8.2：表格法可整表审计/可直接上 MCU/与安全屏蔽及覆盖掩码天然组合/样本效率四条理由，Policy-Based 的适用场景与边界，以及"未来若需连续增益目标优先加密动作网格、仅状态维度大幅扩展才上策略网络"的显式建议。
3. **公式/伪代码 + 一次 rollout 走查（worked example）** —— 新增 §4.8.3，按"一句话直觉 → 更新公式 → 微型地图 → 逐步走查表 → 点破 3 个关键现象 → 假设说明"的顺序：
   - 数据取自封存 `archive/outputs_review_v3/rl_training_transitions.csv` **第 1 回合**（训练场景 #32、IMC 基准 Kp=0.5175/Ki=0.004054、ε=0.25），非构造示例；
   - 微型地图：5×5（ė × e）格子 + 落入次数（23/48 步停在"带内+平稳"格），地图读法给出冷启动→冲过设定值→部分负荷拉回的整回合叙事；
   - 逐步表：前 6 个决策的 e/ė 原始值、落格、动作、目标比例、±10% 信任域钳制后的实际 Kp、奖励、Q 前/后值；
   - 三个关键现象：① Q 表从"抄奖励"开始、第 6 步自举首次生效（td −5.40 ≠ 奖励 −4.67，差值来自第 5 步刚写入的 Q）；② 动作是目标不是命令（±10% 信任域逐格逼近）；③ 安全屏蔽先于探索（偏冷 4 档白名单，随机也出不了安全圈）；
   - 假设说明：α=0.12（噪声下的小步长平均）、γ=0.94（有效视野 ≈ 83 min，刻意短于 240 min 回合）、ε 0.25→0.03 线性退火，均注明"v3 封存运行固定未再调优、解释为与代码行为一致的工程依据"。

### 连带同步

- 分报告第 09 篇：在"跟着代码走一遍"与"结果应该怎么看"之间新增"这一局的地图和记分牌"（三要素 + 为什么 Q 学习，口语化压缩版）与"跟着一局 rollout 走一遍"（同一回合、同一数字）。
- `docs/ALGORITHM_GUIDE.md` §5：状态空间代码块补分箱边界与 9 动作组合、新增"为什么是表格式 Q 学习"段、补 α/γ/ε 取值与理由、加指向 §4.8.3 的完整走查引用。
- 综述版 `实验报告_AI自动整定.md` §3.2：表格后加一句指向详解版 §4.8.1–4.8.3，不展开。
- `docs/主报告.md`（v4 密封批次）与全部代码文件**未改动**。

### 校验

- 走查数字溯源：用 α=0.12、γ=0.94 对第 1 回合全部 48 条转移逐行重放（含末步停止自举的终端处理），Q 前后值与 CSV `td_error` 列**逐条一致（48/48）**；逐步表、微型地图、9 动作表、分箱边界、α/γ/ε 全部与封存 CSV 和 `ai_controllers.py:564-574,698,725` 核对一致。
- 详解版图号未重排（新内容均为 §4.8 内小节插入，图 9 及后续编号不变）。

### 产物

- 报告：`reports/实验报告_AI-PI整定.md`（§4.8.1–4.8.3 新增）、`reports/分报告/09_RL在虚拟房间里学习调参策略.md`（两节新增）、`reports/实验报告_AI自动整定.md`（一句引用）、`docs/ALGORITHM_GUIDE.md`（§5 扩充）
- docx：重新生成 `python reports/md2docx.py 实验报告_AI-PI整定.md` 成功，无公式渲染告警（首轮曾因 `$\le 5\%$` 触发 mathtext 告警，改为纯文本"≤5%"后通过）

---

## 第 4 轮处理记录（2026-09-09）

### 意见：以 `示例.md`（4×4 GridWorld 价值迭代 + Q-Learning 教学）为蓝本改进报告 —— 已处理

用户认为示例报告"实验流程清晰、关键代码好读"，要求以其为蓝本改进。提炼六个可移植技法并落地：

1. **先手算立检查标准**：走查前先手算第 1 步奖励构成（平均 |e|≈4.80、舒适带 1.5×4.30 → ≈−11.26），与封存 CSV 实际值 −11.24 差 0.02（线性近似 + 增益微调罚项 ≈0.013）——手算与程序互证后才往下写。
2. **关键代码贴文出现**：三段逐字摘录（标行号）放进叙事推进位置——分箱 `_bin`/`_propose`（`ai_controllers.py:600-601,637-640`）、TD 更新含终端处理（:783-786）、±10% 信任域 `np.clip`（:746-751）。
3. **自举微型手算**："一步只能传一格"：第 5 步写入 Q[(0,3,0),3]=−0.77，第 6 步 td=−4.67+0.94×(−0.77)=−5.40——与示例 V₁(3,1)=−0.01→V₂(3,1)=0.94 同构。
4. **验证闭环**：新增"自己动手验证"小节（09 篇；详解版为同名段落）：①一行 pandas 核对走查行；②末步终端检查（奖励 −0.2855、td −0.1747 → Q 前值 −0.1108）；③整表复现。
5. **ε 混淆点破**：图 9-4 图注补"示例中 ε=0.30 训练分低但关探索照样最短路与本图是同一个混淆"。
6. **价值表逐轮快照**：新增 `fig_rl_policy_snapshots()`（2×2：第 1/50/450 回合贪心策略快照 + 最终部署表；cmd_bin=1；格子文字=(Kp,Ki) 目标×IMC，颜色=Kp 比例单色三档，未到访灰格；argmax 在安全屏蔽子集内取；WCAG 对比度自检 13.0/8.2/4.5:1）。

### 重要发现（本轮验证产出，如实披露）

- **36000/36000**：α=0.12、γ=0.94 重放全部封存转移（含末步终端处理），td_error 与 CSV 逐条一致——训练轨迹可由公开 CSV 完整重建。
- **部署 Q 表不是学到的表**：重放终表与 `rl_q_table.npy` 不等（最大差 54.6、argmax 一致 28%）；但 `rl_q_table.npy` 与三行构造式（`boot[:,:,:,4]=1.0; boot[3:,:,:,2]=2.0`）**逐元素精确相等**——v3、v4 两次封存运行的策略族选择都选中保守 `baseline_bootstrap_hot_bin_3`（SPIBB 风格），学习检查点在同种子（40_000 系列）验证对比中落败。同种子可比数字：bootstrap 31.95 < IMC 基线 32.91；"最佳检查点 32.77"来自另一种子系列（50_000），不可与 31.95 直接相减。以上已写入 09 篇与详解版（此前仅 09 篇一处括号提及）。

### 修改落点

- `reports/分报告/09_RL在虚拟房间里学习调参策略.md`：走查小节重构（手算→分箱贴码→微型地图→走查表[目标/实际两列合并]→自举手算+TD 贴码→三现象[信任域贴码]→假设说明→两图）；新增"自己动手验证"；图号重排 9-1…9-8（grep 确认无外部文档引用旧号）。
- `reports/实验报告_AI-PI整定.md` §4.8.3：同步手算互证、三段贴码、自己动手验证段（不新增图，文字指向 09 篇图 9-1/9-2）；连带修正 §4.8 正文与图 9 图注中"早停选中 31.95 检查点"的旧口径（31.95 实为策略族选择选中的 bootstrap）。
- `reports/make_s4_figures.py`：修复 suptitle 过时"图 8-3"编号与 docstring；新增 `fig_rl_policy_snapshots()`；重新生成 `fig_rl_q_evolution.png`、`fig_rl_policy_snapshots.png`（只读封存 CSV/npy，不回写）。
- `docs/主报告.md`（v4 密封）与全部代码文件未改动。

### 校验

- 走查 19 个数字与第 3 轮一致仍逐条对上；第 1 步奖励手算差 0.02（已注明构成）；末步终端倒推自洽。
- `fig_rl_policy_snapshots` 脚本内自检：部署表 == bootstrap 构造式（np.array_equal）、对比度、屏蔽内 argmax 分布均通过；渲染后目检两图无误。
- 09 篇图号 9-1…9-8 连续；详解版图 1–27 编号未动。

### 产物

- 报告：`reports/分报告/09_RL在虚拟房间里学习调参策略.md`、`reports/实验报告_AI-PI整定.md`
- 图片：`reports/分报告/figures/fig_rl_q_evolution.png`、`fig_rl_policy_snapshots.png`
- 脚本：`reports/make_s4_figures.py`
- docx：`实验报告_AI-PI整定.docx` 重新生成成功、无公式渲染告警；09 分报告 docx 因原文件被 Word 占用另存为 `09_RL在虚拟房间里学习调参策略.new.docx`（关闭 Word 后重跑 `python reports/md2docx.py 分报告/09_RL在虚拟房间里学习调参策略.md` 覆盖）
